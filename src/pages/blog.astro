---
import MainLayout from '../layouts/MainLayout.astro';
import { gregorian_to_jalali, getJMonth } from '../utils/dateConverter';

// const dscSortPosts = posts.sort((obj1, obj2) => {
//   return new Date(obj2.frontmatter.publishDate) - new Date(obj1.frontmatter.publishDate)
// })
---

<MainLayout title='بلاگ'>
  <section class='pt-12'>
    <div class='pb-28 text-center'>
      <h1 class='pb-4 text-3xl text-zinc-950'>بلاگ</h1>
      <p>
        فعلا اینجا چیزی منتشر نمی کنم :( پست های زیر داخل ویرگول یا مدیوم منتشر
        شده
      </p>
    </div>
    <div
      class='grid grid-cols-1 gap-x-6 gap-y-8 sm:grid-cols-2 lg:grid-cols-3 lg:gap-y-10'
      id='postsContainer'>
    </div>

    <script src='../scripts/getPosts.js'></script>
  </section>
</MainLayout>
